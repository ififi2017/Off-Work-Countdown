# 3.1.9 最终验收记录

日期：2026-09-06。对象：`fa17465` 所在工作区及其中已有的未提交改动；本次新增修复也包含在最后一轮 Web/Desktop 检查中。保留用户原有 Swift、翻译、图片和工程修改，没有提交、推送、上传、送审或发布。

## 发布判断

自动化和已完成的设备画面检查通过，**尚未签署正式发布通过**。TestFlight Production 同步、真实购买/恢复、通知后台周期和完整触摸/无障碍矩阵仍缺实测证据。不要把本地检查通过或开发包安装成功解释为这些门禁已经完成。

## 本轮修复

- Desktop 的两处页面切换类存在 Tailwind 歧义，生成产物退回默认 150ms / 默认缓动。改为明确的 arbitrary property，保留原设计的 340ms 与 `cubic-bezier(0.32,0.72,0,1)`，仍受 `motion-safe` 限制。最终 Web/Desktop CSS 已确认生成目标属性，构建警告消失。
- `recordsDataStatusLabel` 只检查同步开关，却显示“已同步 / Up to date”。将 19 个 locale 的既有 `syncStatusReady` 文案改为“已开启 / On”及对应译文，避免在没有服务端完成证据时宣称同步完成。没有改变同步或冲突逻辑。
- 将 Xcode 工程 `objectVersion` 恢复为仓库约定的 77；保留其他原有工程改动。未提高上传构建号。
- 修正 IAP 审核说明的旧隐私边界，新增 iOS 3.1.9 商店配置并更新操作文档。商店配置引用现有 en-US、zh-Hans、zh-Hant 媒体，没有为其他语言新增媒体集。
- 在 `doneat-site` 对齐英文/简体中文隐私、关于、下载、FAQ、使用说明和移动端对比文案：默认本地，iPhone/iPad 可选私人 iCloud 同步及用户触发的备份导出。网页、Mac、Windows 行为不变。官网修改仍在本地，尚未部署。

## 已完成检查

| 检查 | 结果 | 本机证据 |
| --- | --- | --- |
| ESLint | 通过 | `/tmp/owc-319-lint-copy-final.log` |
| 共享单元测试 | 27 文件，357 项通过 | `/tmp/owc-319-test-copy-final.log` |
| iOS 完整测试 | 8 suites，419 项通过 | `/tmp/owc-319-ios-verified.log`、`/tmp/owc-319-ios-verified.xcresult` |
| iOS 规则与工程检查 | 重建规则、check:ios 通过 | `/tmp/owc-319-rules-copy-final.log`、`/tmp/owc-319-check-ios-copy-final.log` |
| 版本检查 | 各目标 3.1.9；MSIX 3.1.9.0 | `/tmp/owc-319-version-copy-final.log` |
| Web | build / 输出验证通过，54 静态页 | `/tmp/owc-319-web-build-copy-final.log`、`/tmp/owc-319-web-check-copy-final.log` |
| Desktop | export / 输出验证通过，71 静态页 | `/tmp/owc-319-desktop-build-copy-final.log`、`/tmp/owc-319-desktop-check-copy-final.log` |
| Rust | fmt 通过；20 项测试通过 | `/tmp/owc-319-rust-fmt.log`、`/tmp/owc-319-cargo-test.log` |
| Widget 合约 | 12 项测试通过 | `/tmp/owc-319-widget-contract.log` |
| macOS GitHub 渠道 | Release app 构建通过，arm64，ad-hoc 签名符合约定 | `/tmp/owc-319-tauri-build-copy-final.log` |
| iPad 开发包 | 签名构建、保留数据安装、启动成功 | `/tmp/owc-319-ipad-build.log` 与本轮 CoreDevice 输出 |
| iOS Simulator 最终编译 | 最后文案修改后 generic Simulator 编译通过 | `/tmp/owc-319-ios-build-copy-final.log` |
| iOS Release Archive | 最后文案修改后重归档通过；Apple Development 签名，非正式分发包 | `/tmp/owc-319-release-archive-copy-final.log`、`/tmp/owc-319-release-validation.xcarchive` |
| ASC 3.1.9 本地配置 | 17 locales、18 screenshots、3 previews | `/tmp/owc-319-asc-319-check.log` |
| IAP 本地配置 | 17 group / 51 product localizations | `/tmp/owc-319-asc-iap-check.log` |
| 官网 | Astro check 无诊断；build 121 页 | `/tmp/owc-319-doneat-site-check.log`、`/tmp/owc-319-doneat-site-build.log` |
| 翻译 / 补丁完整性 | 19 个 UI locale 键结构一致、新键非空；两仓库 diff --check 通过 | 本轮审查与工具输出 |

日志与构建产物位于本机临时目录，不随 Git 提交。iOS 测试在 iPhone 17 Pro / iOS 26.5 模拟器运行，关闭并行测试以避免测试克隆设备启动问题。

## 设备与画面验收

### macOS

使用本轮构建的 `src-tauri/target/release/bundle/macos/DoneAt.app`，经原生窗口实际操作：主页 → 设置 → 统计 → 返回；深色中文、浅色英文、长英文标签和设置内部滚动。430px 窗口未出现明显裁切或异常增高，主页面固定底栏正常。CSS 参数验证与页面往返均通过；没有声称逐帧性能测量。验收后恢复简体中文和 System 主题。

### iPad mini 6 真机

设备已配对、启用开发者模式，运行 iPadOS 27.0 Beta（24A5430a）。先安装当前代码的 Debug 开发包；随后保留数据安装本轮归档的 Release 包，并在零 QA 参数下成功冷启动。最后的 19 语言状态文案修正已于 23:26 重新归档，并于 23:27 重新安装和零 QA 参数启动；归档内 locale 目录与工作区逐文件比对一致。通过 QuickTime 有线屏幕预览实际查看：

- 默认欢迎页；
- 中文浅色倒计时主页与侧栏；
- 中文浅色记录月视图及左右内容布局；
- 中文深色设置页；
- 中文浅色 Plus 已订阅页面及可见条款/隐私入口；
- 英文浅色 Hours & schedule 详情页、唯一标题和长标签。
- Release 正常冷启动后的中文浅色设置页与 3.1.9 版本显示。

这些画面未见明显标题重叠或内容裁切。通过临时启动参数进入现有页面，未使用会播种记录的 `qaRecordsScale` / `qaRecordsRoute` / Focus 场景，也未发起购买、删除或导入。Plus 页显示已订阅状态只证明该状态的布局，不证明本轮完成 StoreKit 恢复或购买。

QuickTime 只提供画面预览，不能执行 iPad 触摸。CoreDevice 明确返回该设备不支持 `Device Orientation`；Xcode 截图与 Accessibility Inspector 未取得可用的交互检查结果。设备镜像 URL 另被浏览器 URL 策略拦截，未绕过。**因此不能签收连续 push/pop、点击命中、旋转、多任务、VoiceOver 或大字体。** iPadOS 27 Beta 的结果也不替代稳定版 iPadOS 26 验收。

Release 启动后“记录与数据”显示“1 项待处理”，数分钟后的观察仍如此；此前 Debug 页面显示“已同步”。源码复核确认“待处理”来自 `records.state.sync.conflicts.count`，因此这代表 **一条同步冲突，而非待上传队列**。本轮切换过主题，偏好冲突是可能原因，但尚未读取冲突内容确认；不能据此判定数据丢失，也不能签收同步成功。需要由用户在数据设置中确认冲突双方并决定保留/合并哪份数据；本轮没有替用户做选择。

### Archive 权限核对

归档为 3.1.9（2）、arm64、Team `3GSK5B9S3T`，使用 Apple Development。Host 带 `aps-environment=development`、CloudKit 服务与 `iCloud.com.rainif.offworkcountdown.macappstore` 容器；Host/Widget 均带 iOS App Group `group.com.rainif.offworkcountdown.macappstore`、同一 Team 和 `get-task-allow=true`。两个 bundle id 正确。它可以用于本机开发验收，**不能据此签收 App Store 分发权限或 Production CloudKit**；正式分发需重新核对最终产物。

本机 `codesign --verify` 对 Host/Widget 返回 `CSSMERR_TP_NOT_TRUSTED`，本机开发证书信任链检查未通过。Xcode 的内嵌组件检查、实际 iPad 安装与冷启动已成功。这些结果需分别保留，不能把设备接受开发包写成正式分发签名已验收。

### App Store Connect 远端边界

初次只读调用因缺少凭据配置停止；用户提供配置后，已完成认证只读核对，私钥和令牌没有写入报告或日志：

- iOS 3.1.9 的 App Store version 尚未创建，但 TestFlight prerelease train 已存在。
- 3.1.9 已有有效、未过期构建 33、35–54，最新是 54。本轮本机 build 2 仅用于开发验收；新上传应使用高于 54 的构建号（按本次查询，下一号可从 55 起，上传前再次核对）。构建 54 不能代表包含本轮新增修复的最终候选。
- iOS/macOS 均没有版本在审核中，未发现共享 App Info 的审核锁定。
- 月订阅、年订阅和终身购买均为 `READY_TO_SUBMIT`，不是已经通过审核。月/年周期正确，终身购买启用 Family Sharing。
- 本地 3.1.9 plan 会创建版本及 17 个 version localizations，并将 17 个共享隐私 URL 指向 doneat.app。该次远端 plan 没有比较截图/预览；本地媒体格式校验已通过。
- IAP plan 有 6 项差异：三个商品各自的审核说明和审核截图。远端现有截图状态为 COMPLETE，但与本地文件校验和不同。

证据：`/tmp/owc-319-asc-remote-verified-metadata-plan.log`、`/tmp/owc-319-asc-remote-verified-iap-plan.log`、`/tmp/owc-319-asc-remote-verified-state.log`、`/tmp/owc-319-asc-remote-verified-review-locks.log`。没有调用 apply、upload 或 submit。

### 模拟器截图矩阵边界

完整双设备矩阵在 QA iPad 初始化阶段失败，未产生可验收图片。`bootstatus` 持续显示 Data Migration；原生 Simulator 窗口仍是 Apple 启动进度条；重试最终在 `simctl install` 退出。这是应用安装前的环境失败，不能据此认定产品启动失败，也不能把它记成通过。随后将既有脚本复制到临时目录，仅过滤为 iPhone，仍被 CoreSimulator 的列表/安装操作阻塞。`/tmp/owc-319-checks/shots` 与 `phone-shots` 均为 0 张有效图片。任务拥有的截图进程已停止、任务创建的模拟器已关闭；没有清空模拟器数据，没有修改仓库内截图脚本。

## 待本人确认 checklist

- [ ] **真实触摸与可访问性**：iPad 横竖屏/侧栏显隐/多任务，设置与专注连续进入返回；记录单击、双击、浮窗附近日期和纵向滚动互不干扰。大字体、VoiceOver、减少动态效果下入口可达；小屏订阅页滚到底，条款和隐私链接可点。
- [ ] **TestFlight Production 同步**：先导出备份，并在 iPad“记录与数据”中检查本轮观察到的一条冲突，确认保留/合并哪份数据。旧设备保持关闭/离线，在另一台同 iCloud 设备独立恢复；确认记录、薪资和设置一致，而通知授权、设备保护和实时活动开关不会跨设备覆盖。再测断网冲突、删除后旧记录不复活、双端开启专注的收敛。
- [ ] **StoreKit 沙盒**：年度试用、月度无试用、终身购买与现存订阅提示；恢复购买、冷启动、离线跨到期和退款/撤销后权益正确，免费记录/导出/删除可用。Ask to Buy 与宽限期按已有计划补齐。不要使用真实扣款替代沙盒测试。
- [ ] **锁屏/后台**：临近测试班次锁屏送达；停止或改排班后旧提醒消失；专注→短休→下一轮→长休完整周期；工作与专注实时活动优先级正确。iPad 不支持的 Live Activity 表面需在 iPhone 验收；Widget 时间线及恢复前台后一致。
- [ ] **正式候选与商店**：把本轮及既有改动纳入可复现候选并通过 main 的 CI；使用高于远端最新 54 的新构建号（当前本地开发产物为 2），检查正式分发 Archive 的 App/Widget entitlements、CloudKit Production、App Group 和 APNs。创建 3.1.9 商店草稿，复核更新说明/媒体，关联三个 READY_TO_SUBMIT 的 IAP 并同步其审核资料；核对隐私问卷，部署官网隐私更新后再提交。
- [ ] **若同时发布 Desktop**：Windows x64/ARM64 安装与升级、通知、Mini Timer 和渠道专属包实测；Mac App Store 单独验 Team-ID 前缀 App Group、Widget 与分发签名。此轮 macOS GitHub 包不能代替这些渠道。

较完整的操作步骤见 `2026-09-05-3.1.9-release-closeout.md`；本文件的最新实测数量和范围优先于旧记录。
