# 3.1.9 人生、首次恢复与 iPad 导航复核

## 自动检查

- 共享规则：355 个测试通过（27 个文件），包含月薪分摊及未来收入递减边界。
- iOS：404 个测试通过（6 个 suite），包含稳定阶段身份、旧档案兼容、首次恢复候选提交、离线配对门禁、跨 generation 本机记录保护及延迟权益恢复。
- lint、Web build / validation、Desktop export / validation 通过。
- 规则包重新生成；iOS simulator build、check:ios、check:version 通过，产品版本仍为 3.1.9。

## 已进行的模拟器检查

- iPhone：人生工作已走过、未来推算均可选中，选中说明不会因计时刷新消失。
- iPhone：人生编辑器的保持当前收入 / 逐步递减切换、45 岁 / 60% 可编辑字段及说明布局。
- iPhone：iCloud 未登录时显示检查失败，明确选择本机配置后才进入欢迎流程。
- iPad：最终版本横屏浅色中文、竖屏深色英文均实际检查；记录页标题和全部记录入口可见，侧栏 Logo、DoneAt、班次倒计时与阶段说明已恢复。
- iPad：实点设置 → 排班、计时 → 专注、标签切换后的原路径恢复和返回；二级页收起侧栏后顶部导航与返回控件各占独立空间。
- 录屏逐帧检查：侧栏退向左侧，顶部 Tab 从上方进入，未沿侧栏位置飞移；录屏保存在本机 `/tmp/owc-restore-shots/ipad-navigation-verified.mp4`，未入库。
- 检查中发现并修复重复系统侧栏按钮、叠放多个导航栈导致设置入口无响应、顶部栏被二级页覆盖，以及 inset 遮挡返回按钮的问题。最终顶部栏与整个原生 Split 采用外层 VStack 分配实际布局空间。

## 仍需真实环境验收

- TestFlight Production：旧设备不启动时，新设备独立下载已上传设置；实际订阅恢复、退款与过期。
- iCloud 空账号、断网、切换账号、跨 generation 有本机记录时备份并选择云副本的完整真机流程。
- iPad 真机侧栏左右开合、顶部栏上下进出与设置 / 番茄钟连续 push/pop 的最终手感；Reduce Motion、动态字体及多任务窄窗。

模拟器无真实 iCloud 账号，候选恢复单元测试不代表 Production 云端端到端恢复成功。未执行真实云数据删除，也未上传 TestFlight 包。
