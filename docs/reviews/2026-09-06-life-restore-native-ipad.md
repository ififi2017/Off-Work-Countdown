# 3.1.9 人生、首次恢复、固定月收入与 iPad 导航复核

## 最终行为

- 人生工作阶段使用稳定身份；未来收入递减按用户确认的起始年龄 / 退休收入比例计算，旧档案默认不变。
- 首次启动先只读检查 iCloud，选定云副本完整校验后原子恢复；离线配置保留首次配对门禁，跨 generation 的本机数据先备份再明确替换。
- 记录月收入按日历日分摊，不按少记工时、请假或缺勤扣款；保留年终奖平摊。实际工时仍按记录统计，日薪模式不变。
- iPad 侧栏按钮和三个 Tab 共用一个固定居中的玻璃栏，根页面与二级页之间不重建或移动这条栏。
- iPad 根页面和二级页都隐藏系统 navigationBar。二级页由固定内容页头提供返回、标题与操作按钮，避免 push 中途显示大标题导航栏并二次调整安全区。iPhone 仍使用原有原生导航。
- 计时页收起侧栏时跨越窄 / 宽布局阈值采用定向交叠淡变；展开方向保持现有效果。

## 自动检查

- 最终固定月收入及年终奖口径：357 个共享规则测试通过（27 个文件）。
- iOS：404 个测试通过（428 次含参数化执行），包含完整月份收入原生桥接、人生身份与递减、首次恢复门禁和候选提交。
- lint、Web build / validation、Desktop export / validation 通过。
- iOS 规则包重新生成；最后的 iPad 固定页头改动通过 simulator build、check:ios、check:version、diff-check，版本仍为 3.1.9。

## 模拟器实际检查

- iPhone：人生两个工作阶段选择可保持；递减输入与说明；无 iCloud 账号时明确选择后才进入欢迎配置。
- 最终 iPad 竖屏浅色中文：排班 push/pop，侧栏开合，计时 → 专注 → 返回，设置与记录入口，全部记录二级页。
- 固定玻璃栏在根页面、排班与专注二级页位置和样式相同；返回 / 保存及专注新建 / 设置按钮保留。
- 排班 push 录屏逐帧检查末段：标题随整页进入固定边距，未观察到贴侧栏后再回弹。录屏 `/tmp/owc-restore-shots/ipad-fixed-header-final.mp4` 与逐帧图在本机，未入库。
- iPhone 深色英文排班页：原生大标题、返回、保存及底部 Tab 保持既有形式。
- 更早版本已检查 iPad 横屏浅色中文 / 竖屏深色英文记录页；最终固定页头的真机横竖屏手感仍需验收。

## 仍需真实环境验收

- TestFlight Production：旧设备不启动时，新设备独立恢复已经上传的设置；实际订阅恢复、退款与过期。
- 空账号、断网、切换账号、跨 generation 本机记录的备份和选择云副本完整流程。
- iPad 真机侧栏、二级页连续 push/pop，动态字体、Reduce Motion 和多任务窄窗。

模拟器无真实 iCloud 账号，候选恢复单元测试不代表 Production 端到端成功。未删除真实云数据，未上传 TestFlight。

## 合并关系

#128 被合并到了 #127 的旧功能分支，并未进入 main。本次后续分支已纳入最新 main，新的 PR 直接以 main 为 base，同时带入 #128 的修复与本轮后续反馈，不再要求串联合并旧分支。
